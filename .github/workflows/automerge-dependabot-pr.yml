name: Automerge Dependabot PRs

on:
  workflow_dispatch:
  schedule:
    - cron: '0 7 * * MON-FRI'  # Kjør hver arbeidsdag kl 08:00

jobs:
  automerge:
    permissions:
      contents: write
      pull-requests: write
      statuses: read
      id-token: write
    uses: navikt/dok-workflows/.github/workflows/automerge-dependabot-pr.yml@main
    secrets: inherit